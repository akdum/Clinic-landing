import * as React from "react";
import * as ReactDOM from 'react-dom';

export class Carousel extends React.Component<null, null> {
    render() {
        return (
            <div className="container-fluid">
            </div>
        );
    }
}